// Match score at/above which a job counts as a "strong match" — used for
// the Find Jobs success banner and the High Match filter (Feature 11).
// Import this everywhere the threshold is needed — never hardcode 70.
export const MATCH_THRESHOLD = 70;
